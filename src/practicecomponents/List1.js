import React, {Component} from 'react'

function List1 ({p}){
 
     
     //const plist = p.map(p => )
        return (
            <div>
            <h2>I am {p.name} of age {p.age} </h2>
            </div>
        );
        
    }


export default List1